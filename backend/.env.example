# Example environment configuration for the backend

# OpenAI API key used to connect to the Realtime API
OPENAI_API_KEY=

# Service account for Firebase Admin SDK encoded as a base64 JSON string.
# Generate this file in the Google Cloud console (IAM & Admin > Service Accounts > Keys).
FIREBASE_SERVICE_ACCOUNT_BASE64=

# Example Twilio credentials for a premium business.  Replace `abc123` with your business ID.
# Do NOT check your real auth token into source control.
TWILIO_BUSINESS_abc123=
# Port on which the backend listens.  Defaults to 5050.
PORT=5050
